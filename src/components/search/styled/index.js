import styled from 'styled-components'

const SearchContainer = styled.div`
  padding: 60px 0;
`

const SearchForm = styled.form``

const Input = styled.input``

const Select = styled.select``

export {
  SearchContainer, SearchForm, Input, Select
}